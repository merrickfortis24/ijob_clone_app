This folder should contain your font files.

Place the real 'Signatra.ttf' file here (rename if necessary) so it matches the
path referenced in `pubspec.yaml`:

  assets/fonts/Signatra.ttf

Remove this README after adding the font file.
